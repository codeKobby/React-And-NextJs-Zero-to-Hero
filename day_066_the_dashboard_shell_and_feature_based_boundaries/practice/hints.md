# Day 066 hints: The dashboard shell and feature-based boundaries

Use these after attempting the the numbered exercises in this lesson. They are specific to **The dashboard shell and feature-based boundaries** and should unblock the next thought without replacing it.

## Hints

1. Begin with the learner problem: Learners need a concrete reason to study the dashboard shell and feature-based boundaries before the terminology becomes useful.
2. Run the smallest example unchanged and inspect the evidence for a small local fixture that demonstrates the dashboard shell and feature-based boundaries.
3. Trace the input, operation, output, and owner at the code or framework boundary that owns the decision in this lesson.
4. Change exactly one input related to What makes a dashboard shell?; keep the rule fixed.
5. For How do route groups organize features?, decide the normal and boundary behavior before coding.
6. Reproduce the likely mistake: Copy the syntax without identifying the input, owner, output, and boundary.
7. Repair the smallest line or boundary; do not hide the failure with a broad workaround.
8. Keep the data local and synthetic while you test Where should navigation state live?.
9. Assert a visible result or public contract rather than a private implementation detail.
10. Use the same fixture to apply the dashboard shell and feature-based boundaries to a small local fixture that demonstrates the dashboard shell and feature-based boundaries.
11. A passing build proves only the checked build completed; record what remains untested.
12. Your review note should name the owner, evidence, limitation, and boundary: the code or framework boundary that owns the decision in this lesson.
