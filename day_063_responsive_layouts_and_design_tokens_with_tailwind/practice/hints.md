# Day 063 hints: Responsive layouts and design tokens with Tailwind

Use these only after attempting the numbered exercises in [the lesson](../day_063_responsive_layouts_and_design_tokens_with_tailwind.md). They are specific to **Responsive layouts and design tokens with Tailwind** and should unblock the next thought without replacing it.

## Hints

1. Read the short problem statement for responsive layouts and design tokens with tailwind and say what the page or program should do.
2. Run the example without changes. If it works, write down what you see for a local dashboard shell with a readable, keyboard-usable Button and empty state.
3. Point to the input and the line that changes the output. Do not try to explain the whole application at once.
4. Change one input for What is responsive design?. Keep every other line the same.
5. For How do Tailwind breakpoints change a layout?, try the normal case first. Then try one empty or bad value.
6. Make this mistake in a copy: Copy a configuration from the wrong major version, scatter arbitrary colors, or treat generated component source as a black box.
7. Read the error or wrong result. Fix one line. Run the example again.
8. Use invented local data while you try What is a design token?.
9. Check the text, number, URL, or DOM element that the learner can actually see.
10. Use the same starter to build a local dashboard shell with a readable, keyboard-usable Button and empty state. Do not add a new package.
11. A passing check only proves that check passed. Write one thing you did not test.
12. If a technical word is confusing, define it in your own simple sentence before using it.
