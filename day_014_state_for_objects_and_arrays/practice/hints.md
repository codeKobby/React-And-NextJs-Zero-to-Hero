# Day 014 hints: State for objects and arrays

Use these only after attempting the numbered exercises in [the lesson](../day_014_state_for_objects_and_arrays.md). They are specific to **State for objects and arrays** and should unblock the next thought without replacing it.

## Hints

1. Begin with the learner problem: Learners need a concrete reason to study state for objects and arrays before the terminology becomes useful.
2. Run the smallest example unchanged and inspect the evidence for a small local fixture that demonstrates state for objects and arrays.
3. Trace the input, operation, output, and owner at the code or framework boundary that owns the decision in this lesson.
4. Change exactly one input related to Why should state updates create new references?; keep the rule fixed.
5. For How do we update one object field?, decide the normal and boundary behavior before coding.
6. Reproduce the likely mistake: Copy the syntax without identifying the input, owner, output, and boundary.
7. Repair the smallest line or boundary; do not hide the failure with a broad workaround.
8. Keep the data local and synthetic while you test How do we add and remove array items?.
9. Assert a visible result or public contract rather than a private implementation detail.
10. Use the same fixture to apply state for objects and arrays to a small local fixture that demonstrates state for objects and arrays.
11. A passing build proves only the checked build completed; record what remains untested.
12. Your review note should name the owner, evidence, limitation, and boundary: the code or framework boundary that owns the decision in this lesson.
