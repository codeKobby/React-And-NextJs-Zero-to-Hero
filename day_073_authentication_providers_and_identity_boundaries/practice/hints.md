# Day 073 hints: Authentication providers and identity boundaries

Use these only after attempting the numbered exercises in [the lesson](../day_073_authentication_providers_and_identity_boundaries.md). They are specific to **Authentication providers and identity boundaries** and should unblock the next thought without replacing it.

## Hints

1. Read the short problem statement for authentication providers and identity boundaries and say what the page or program should do.
2. Run the example without changes. If it works, write down what you see for a local protected case route with synthetic sessions, permissions, and unauthorized fixtures.
3. Point to the input and the line that changes the output. Do not try to explain the whole application at once.
4. Change one input for What is authentication?. Keep every other line the same.
5. For What belongs to an authentication provider?, try the normal case first. Then try one empty or bad value.
6. Make this mistake in a copy: Treat a browser field, client redirect, or login flag as proof of permission and return data before the server policy runs.
7. Read the error or wrong result. Fix one line. Run the example again.
8. Use invented local data while you try Why is identity different from permission?.
9. Check the text, number, URL, or DOM element that the learner can actually see.
10. Use the same starter to build a local protected case route with synthetic sessions, permissions, and unauthorized fixtures. Do not add a new package.
11. A passing check only proves that check passed. Write one thing you did not test.
12. If a technical word is confusing, define it in your own simple sentence before using it.
