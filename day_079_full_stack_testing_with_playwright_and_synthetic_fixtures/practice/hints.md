# Day 079 hints: Full-stack testing with Playwright and synthetic fixtures

Use these only after attempting the numbered exercises in [the lesson](../day_079_full_stack_testing_with_playwright_and_synthetic_fixtures.md). They are specific to **Full-stack testing with Playwright and synthetic fixtures** and should unblock the next thought without replacing it.

## Hints

1. Read the short problem statement for full-stack testing with playwright and synthetic fixtures and say what the page or program should do.
2. Run the example without changes. If it works, write down what you see for a local synthetic case journey with normal, invalid, empty, and failure fixtures.
3. Point to the input and the line that changes the output. Do not try to explain the whole application at once.
4. Change one input for What should each test level prove?. Keep every other line the same.
5. For How do we test a protected route?, try the normal case first. Then try one empty or bad value.
6. Make this mistake in a copy: Assert a private implementation detail while skipping the visible contract the learner actually needs to protect.
7. Read the error or wrong result. Fix one line. Run the example again.
8. Use invented local data while you try What is a safe fixture?.
9. Check the text, number, URL, or DOM element that the learner can actually see.
10. Use the same starter to build a local synthetic case journey with normal, invalid, empty, and failure fixtures. Do not add a new package.
11. A passing check only proves that check passed. Write one thing you did not test.
12. If a technical word is confusing, define it in your own simple sentence before using it.
